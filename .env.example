# Copy to .env and fill in. docker compose passes these to pretalx.
# pretix: Organizer -> Customer accounts -> SSO clients
PRETALX_PRETIX_SSO_CLIENT_SECRET=
# pretix: Organizer -> Teams -> API tokens (team needs "Can view orders")
PRETALX_PRETIX_SSO_API_TOKEN=
